import SwiftUI

struct ContentView: View {
    var body: some View {
        PiStickWebView()
            .background(Color(red: 0.035, green: 0.035, blue: 0.043))
            .ignoresSafeArea()
    }
}
